name: agent

x-agent-common: &agent-common
  restart: unless-stopped
  networks:
    - agent-internal
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

x-agent-environment: &agent-environment
  OPENAI_API_KEY: ${OPENAI_API_KEY}
  OPENAI_MODEL: ${OPENAI_MODEL}
  OPENAI_BASE_URL: ${OPENAI_BASE_URL}
  IMAGE_API_KEY: ${IMAGE_API_KEY}
  IMAGE_BASE_URL: ${IMAGE_BASE_URL}
  IMAGE_MODEL: ${IMAGE_MODEL}
  AGENT_DEADLINE_MS: ${AGENT_DEADLINE_MS:-120000}
  AGENT_DEADLINE_WITH_TOOLS_MS: ${AGENT_DEADLINE_WITH_TOOLS_MS:-300000}
  SERVER_REQUEST_TIMEOUT_MS: ${SERVER_REQUEST_TIMEOUT_MS:-310000}
  LLM_TIMEOUT_MS: ${LLM_TIMEOUT_MS:-30000}
  LLM_MAX_RETRIES: ${LLM_MAX_RETRIES:-1}
  MAX_AGENT_ITERATIONS: ${MAX_AGENT_ITERATIONS:-6}
  AGENT_API_SECRET: ${AGENT_API_SECRET}
  CORS_ORIGIN: ${CORS_ORIGIN:-}
  TAVILY_API_KEY: ${TAVILY_API_KEY:-}
  TAVILY_SEARCH_DEPTH: ${TAVILY_SEARCH_DEPTH:-basic}
  SEARCH_TIMEOUT_MS: ${SEARCH_TIMEOUT_MS:-4500}
  SEARCH_MAX_RESULTS: ${SEARCH_MAX_RESULTS:-8}
  SEARCH_CACHE_TTL_SECONDS: ${SEARCH_CACHE_TTL_SECONDS:-30}
  SEARCH_STALE_TTL_SECONDS: ${SEARCH_STALE_TTL_SECONDS:-600}
  MEMORY_ENABLED: ${MEMORY_ENABLED:-true}
  CLOUDFLARE_MEMORY_BASE_URL: ${CLOUDFLARE_MEMORY_BASE_URL:-}
  CLOUDFLARE_MEMORY_SECRET: ${CLOUDFLARE_MEMORY_SECRET:-}
  NO_PROXY: ${NO_PROXY:-localhost,127.0.0.1}
  no_proxy: ${no_proxy:-localhost,127.0.0.1}

services:
  ts-agent:
    <<: *agent-common
    environment:
      <<: *agent-environment
    profiles: ["typescript"]
    build:
      context: ./ts-langchain
      dockerfile: Dockerfile
    ports:
      - "${TS_PUBLIC_PORT:-6001}:6001"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:6001/api/v1/health"]
      interval: 60s
      timeout: 5s
      retries: 5
      start_period: 20s

  py-agent:
    <<: *agent-common
    profiles: ["python"]
    build:
      context: ./py-langgraph
      dockerfile: Dockerfile
    environment:
      <<: *agent-environment
      HOST: 0.0.0.0
      PORT: "6002"
    ports:
      - "${PY_PUBLIC_PORT:-6002}:6002"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request; urllib.request.urlopen('http://127.0.0.1:6002/api/v1/health', timeout=3)
      interval: 60s
      timeout: 5s
      retries: 5
      start_period: 20s

networks:
  agent-internal:
    driver: bridge
