# Cloudflare Service 环境变量配置
# 复制此文件为 .dev.vars 用于本地开发
# 生产环境变量通过 wrangler secret 或 CI/CD 注入

SERVICE_SECRET=change-me-in-production-use-strong-secret
EMBEDDING_MODEL=@cf/baai/bge-m3
EMBEDDING_DIMENSIONS=1024
MAX_MEMORIES_PER_USER=50
INDEX_JOB_BATCH_SIZE=20
INDEX_JOB_MAX_RETRIES=5
