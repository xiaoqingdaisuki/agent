# LLM Provider
OPENAI_API_KEY=
OPENAI_MODEL=step-3.7-flash
OPENAI_BASE_URL=https://api.stepfun.com/step_plan/v1
IMAGE_MODEL=step-image-edit-2

# Timeout and loop budgets
AGENT_DEADLINE_MS=30000
SERVER_REQUEST_TIMEOUT_MS=40000
LLM_TIMEOUT_MS=12000
LLM_MAX_RETRIES=1
MAX_AGENT_ITERATIONS=6
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# Web search (Tavily is the single search backend)
TAVILY_API_KEY=
TAVILY_SEARCH_DEPTH=basic
SEARCH_TIMEOUT_MS=4500
SEARCH_MAX_RESULTS=8
SEARCH_CACHE_TTL_SECONDS=30
SEARCH_STALE_TTL_SECONDS=600

# API
HOST=0.0.0.0
PORT=6002
CORS_ORIGIN=http://localhost:3000

# file.read 可访问的根目录（默认当前进程目录）
# AGENT_WORKSPACE_ROOT=D:\\agent\\py-langgraph

# Database
POSTGRES_URI=postgresql://agent:agent@localhost:5432/agent
QDRANT_URL=http://localhost:6333
